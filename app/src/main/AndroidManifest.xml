<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.archives">

    <application
        android:name=".common.app.App"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".function.main.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <activity
            android:name=".function.main.activity.CaseInvesDetailActivity"
            android:label="信息详情"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".function.main.activity.ZancunsDetailActivity"
            android:label="信息详情"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".function.main.activity.VerificationsDetailActivity"
            android:label="信息详情"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".function.main.activity.LettersDetailActivity"
            android:label="信息详情"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".function.main.activity.EndingsDetailActivity"
            android:label="信息详情"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".function.main.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>